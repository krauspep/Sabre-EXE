ALTER TABLE ATTENDANCE_CALCULATION_RULE ADD HOURS_ROUNDING DM_BOOLEAN;

UPDATE ATTENDANCE_CALCULATION_RULE SET HOURS_ROUNDING = 0;

ALTER TABLE ATTENDANCE_CALCULATION_RULE ADD ROUND_HOURS DM_SMALLINT;

UPDATE ATTENDANCE_CALCULATION_RULE SET ROUND_HOURS = 5;


