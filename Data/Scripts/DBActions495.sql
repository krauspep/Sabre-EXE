CREATE TABLE OPERATOR_FORM_SETTINGS (
	OPERATOR_KEY INTEGER NOT NULL,
	FORM_NAME    VARCHAR(100),
	ITEM         VARCHAR(100),
	ITEMVALUE    VARCHAR(200),
	PRIMARY KEY ( OPERATOR_KEY, FORM_NAME, ITEM )
)