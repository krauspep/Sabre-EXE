ALTER TABLE EMPLOYEE_ATTENDANCE_DATA ADD PRE_MARTERINITY DM_BOOLEAN;

UPDATE EMPLOYEE_ATTENDANCE_DATA SET PRE_MARTERINITY = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_DATA ADD POST_MARTERINITY DM_BOOLEAN;

UPDATE EMPLOYEE_ATTENDANCE_DATA SET POST_MARTERINITY = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD PRE_MARTERINITY DM_BOOLEAN;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET PRE_MARTERINITY = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD POST_MARTERINITY DM_BOOLEAN;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET POST_MARTERINITY = 0;

ALTER TABLE LEAVE_TYPE ADD APPLY_MATERNITY_RULE DM_BOOLEAN;

UPDATE LEAVE_TYPE SET APPLY_MATERNITY_RULE = 0;

ALTER TABLE LEAVE_TYPE ADD HRS_PERC DM_INTEGER;

UPDATE LEAVE_TYPE SET HRS_PERC = 0;

ALTER TABLE LEAVE_TYPE ADD MIN_ADD DM_INTEGER;

UPDATE LEAVE_TYPE SET MIN_ADD = 0;
