ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD MINUTES_UNAPPROVED_OT1 DM_INTEGER;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET MINUTES_UNAPPROVED_OT1 = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD MINUTES_UNAPPROVED_OT2 DM_INTEGER;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET MINUTES_UNAPPROVED_OT2 = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD OT2_MINUTES_SPLIT DM_INTEGER;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET OT2_MINUTES_SPLIT = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD MINUTES_OT1_ORIG DM_INTEGER;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET MINUTES_OT1_ORIG = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD MINUTES_OT2_ORIG DM_INTEGER;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET MINUTES_OT2_ORIG = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD UNAPPROVE_REASON_KEY DM_KEY_N1;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET UNAPPROVE_REASON_KEY = -1;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD OT1_MINUTES_SPLIT DM_BOOLEAN;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET OT1_MINUTES_SPLIT = 0;




