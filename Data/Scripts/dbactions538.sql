ALTER TABLE SHIFT ADD 
FRI_NIGHT_SHIFT_SPLIT Smallint NOT NULL;

UPDATE SHIFT 
SET FRI_NIGHT_SHIFT_SPLIT = '0' 
WHERE FRI_NIGHT_SHIFT_SPLIT IS NULL;

ALTER TABLE SHIFT ADD 
PPH_SHIFT_SPLIT Smallint NOT NULL;

UPDATE SHIFT 
SET PPH_SHIFT_SPLIT = '0' 
WHERE PPH_SHIFT_SPLIT IS NULL;
