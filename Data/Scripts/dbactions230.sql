ALTER TABLE ATTENDANCE_CALCULATION_RULE ADD CLOCK_ROUNDING DM_BOOLEAN;

ALTER TABLE ATTENDANCE_CALCULATION_RULE ADD ROUND_IN DM_SMALLINT;

ALTER TABLE ATTENDANCE_CALCULATION_RULE ADD ROUND_OUT DM_SMALLINT;

ALTER TABLE ATTENDANCE_CALCULATION_RULE ADD ROUND_BREAK_IN DM_SMALLINT;

ALTER TABLE ATTENDANCE_CALCULATION_RULE ADD ROUND_BREAK_OUT DM_SMALLINT;

UPDATE ATTENDANCE_CALCULATION_RULE SET CLOCK_ROUNDING = 0;

UPDATE ATTENDANCE_CALCULATION_RULE SET ROUND_IN = 0;

UPDATE ATTENDANCE_CALCULATION_RULE SET ROUND_OUT = 0;

UPDATE ATTENDANCE_CALCULATION_RULE SET ROUND_BREAK_IN = 0;

UPDATE ATTENDANCE_CALCULATION_RULE SET ROUND_BREAK_OUT = 0;

ALTER TABLE ATTENDANCE ADD ORIG_ATTENDANCE_TIME DM_TIME;

ALTER TABLE ATTENDANCE_ARCHIVE ADD ORIG_ATTENDANCE_TIME DM_TIME;
