ALTER TABLE ATTENDANCE ADD LINK_ATTENDANCE_KEY DM_KEY_N1;

UPDATE ATTENDANCE SET LINK_ATTENDANCE_KEY = -1;

ALTER TABLE ATTENDANCE ADD CONSOLIDATED DM_BOOLEAN;

UPDATE ATTENDANCE SET CONSOLIDATED = 1;
