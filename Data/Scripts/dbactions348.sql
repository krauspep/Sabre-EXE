ALTER TABLE COMPANY ADD CLOCKING_IMPORT_DONE DM_BOOLEAN;

ALTER TABLE COMPANY ADD CLOCKING_EXCEPTIONS_DONE DM_BOOLEAN;

ALTER TABLE COMPANY ADD SHIFT_SENSING_DONE DM_BOOLEAN;

ALTER TABLE COMPANY ADD CALCULATE_BREAKS_DONE DM_BOOLEAN;

ALTER TABLE COMPANY ADD CALCULATE_TA_DONE DM_BOOLEAN;

ALTER TABLE COMPANY ADD ALLOCATE_UNSCHEDULED_DAYS_DONE DM_BOOLEAN;

ALTER TABLE COMPANY ADD APPROVE_OVERTIME_DONE DM_BOOLEAN;

UPDATE COMPANY SET CLOCKING_IMPORT_DONE = 0;

UPDATE COMPANY SET CLOCKING_EXCEPTIONS_DONE = 0;

UPDATE COMPANY SET SHIFT_SENSING_DONE = 0;

UPDATE COMPANY SET CALCULATE_BREAKS_DONE = 0;

UPDATE COMPANY SET CALCULATE_TA_DONE = 0;

UPDATE COMPANY SET ALLOCATE_UNSCHEDULED_DAYS_DONE = 0;

UPDATE COMPANY SET APPROVE_OVERTIME_DONE = 0;

