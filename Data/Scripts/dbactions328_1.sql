SET TERM ^ ;
CREATE TRIGGER TR_ATTENDANCE_AUDIT FOR ATTENDANCE_AUDIT
ACTIVE BEFORE INSERT POSITION 0
AS
begin
  if (new.ATTENDANCE_AUDIT_KEY is null)
  then begin
         new.ATTENDANCE_AUDIT_KEY = gen_id(ATTENDANCE_AUDIT_GEN, 1);
       end
end^
SET TERM ; ^

