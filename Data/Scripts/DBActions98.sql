ALTER TABLE SYSTEM_COLOUR ADD DUTY_WEEK_DAY_TO_COLOUR DM_COLOUR_NAME;

update system_colour set DUTY_WEEK_DAY_TO_COLOUR = 16777215;

ALTER TABLE SYSTEM_COLOUR ADD DUTY_WEEK_DAY_TO_FONT_COLOUR DM_COLOUR_NAME;

update system_colour set DUTY_WEEK_DAY_TO_FONT_COLOUR = 0;

ALTER TABLE SYSTEM_COLOUR ADD DUTY_UNSCHEDULED_COLOUR DM_COLOUR_NAME;

update system_colour set DUTY_UNSCHEDULED_COLOUR = 16777215;

ALTER TABLE SYSTEM_COLOUR ADD DUTY_UNSCHEDULED_FONT_COLOUR DM_COLOUR_NAME;

update system_colour set DUTY_UNSCHEDULED_FONT_COLOUR = 0;

CREATE INDEX I_WORK_SCHEDULE_1 ON WORK_SCHEDULE(EMPLOYEE_KEY, SHIFT_KEY, ATTENDANCE_DATE_START, ATTENDANCE_DATE_END, DELETED);

CREATE INDEX I_SHIFT_ASSIGN_WEEK_1 ON SHIFT_ASSIGN_WEEK(ASSIGNED_WEEK_NO, SHIFT_KEY, DELETED);

DROP INDEX I_ATTENDANCE_3;

CREATE INDEX I_ATTENDANCE_3 ON ATTENDANCE(ENROLL_ID, COMPANY_KEY, DEVICE_CONFIGURATION_KEY, DEVICE_TYPE_KEY, ATTENDANCE_DATE, ATTENDANCE_TIME, DELETED);

update device_configuration set device_Name = 'Device' where device_name = '' or device_name is null;

update device_configuration set device_type_key = 0 where device_type_key is null;

update device_configuration set device_password = 0 where device_password is null;

ALTER TABLE DEVICE_CONFIGURATION ADD AUTO_DELETE_ACCESS_LOG_DATA DM_BOOLEAN;

UPDATE DEVICE_CONFIGURATION SET AUTO_DELETE_ACCESS_LOG_DATA = 0;

ALTER TABLE DOWNLOAD_AUDIT ADD DOWNLOAD_TIME_END DM_TIME;

ALTER TABLE DOWNLOAD_AUDIT ADD AUTID_NOTES DM_BLOB;

UPDATE ATTENDANCE_STATUS SET ATTENDANCE_STATUS_NAME = 'Unscheduled Day' WHERE ATTENDANCE_STATUS_KEY = 8;

CREATE INDEX I_EMPLOYEE_SHIFT_1 ON EMPLOYEE_SHIFT(EMPLOYEE_KEY, SHIFT_KEY, DELETED);
