ALTER TABLE MEMBER ADD LAST_DEVICE_KEY DM_KEY;

UPDATE MEMBER SET LAST_DEVICE_KEY = -1;

ALTER TABLE PUPIL ADD LAST_DEVICE_KEY DM_KEY;

UPDATE PUPIL SET LAST_DEVICE_KEY = -1;

ALTER TABLE PUPIL_PARENT ADD LAST_DEVICE_KEY DM_KEY;

UPDATE PUPIL_PARENT SET LAST_DEVICE_KEY = -1;





