ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD APPROVED_MINUTES_PAID_LEAVE DM_SMALLINT_0_NULL;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET APPROVED_MINUTES_PAID_LEAVE = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD MINUTES_PAID_LEAVE DM_INTEGER;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET MINUTES_PAID_LEAVE = 0;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD APPROVE_LEAVE_NOTE DM_BLOB;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD OT1_MINUTES_SPLIT_TEMP DM_SMALLINT_0_NULL;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET OT1_MINUTES_SPLIT_TEMP = OT1_MINUTES_SPLIT;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED DROP OT1_MINUTES_SPLIT;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD OT1_MINUTES_SPLIT DM_SMALLINT_0_NULL;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET OT1_MINUTES_SPLIT = OT1_MINUTES_SPLIT_TEMP;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED DROP OT1_MINUTES_SPLIT_TEMP;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD APPROVED_MIN_PDL_TEMP DM_SMALLINT_0_NULL;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET APPROVED_MIN_PDL_TEMP = APPROVED_MINUTES_PAID_LEAVE;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED DROP APPROVED_MINUTES_PAID_LEAVE;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED ADD APPROVED_MINUTES_PAID_LEAVE DM_SMALLINT_0_NULL;

UPDATE EMPLOYEE_ATTENDANCE_AMENDED SET APPROVED_MINUTES_PAID_LEAVE = APPROVED_MIN_PDL_TEMP;

ALTER TABLE EMPLOYEE_ATTENDANCE_AMENDED DROP APPROVED_MIN_PDL_TEMP;





