ALTER TABLE GPRS_DEVICE_CONFIGURATION ADD AUTH_SERVER DM_BOOLEAN;

UPDATE GPRS_DEVICE_CONFIGURATION SET AUTH_SERVER = 0;

ALTER TABLE GPRS_DEVICE_CONFIGURATION ADD AUTH_SERVER_TYPE DM_CHAR;

ALTER TABLE GPRS_DEVICE_CONFIGURATION ADD AUTH_SERVER_URL DM_URL_ADDRESS;

ALTER TABLE GPRS_DEVICE_CONFIGURATION ADD AUTH_SERVER_DNS DM_URL_ADDRESS;

ALTER TABLE GPRS_DEVICE_CONFIGURATION ADD REDIAL_INTERVAL DM_SMALLINT;

UPDATE GPRS_DEVICE_CONFIGURATION SET REDIAL_INTERVAL = 0;

ALTER TABLE GPRS_DEVICE_CONFIGURATION ADD REDIAL_COUNT DM_SMALLINT;

UPDATE GPRS_DEVICE_CONFIGURATION SET REDIAL_COUNT = 0;

