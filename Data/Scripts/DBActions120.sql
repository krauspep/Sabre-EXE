ALTER TABLE EMPLOYEE ADD TRACK DM_BOOLEAN;

ALTER TABLE RESIDENT ADD TRACK DM_BOOLEAN;

UPDATE EMPLOYEE SET TRACK = 1;

UPDATE RESIDENT SET TRACK = 1;

UPDATE COMPANY SET COMPANY_TYPE = 1 WHERE COMPANY_KEY = -1;

UPDATE EMPLOYEE SET EVENT_STATUS = 'OUT';

UPDATE RESIDENT SET EVENT_STATUS = 'OUT';

ALTER TABLE CONTACT_TYPE ADD ALERT DM_BOOLEAN;

ALTER TABLE EMPLOYEE ADD LAST_EVENT_DATE DM_DATE;

ALTER TABLE RESIDENT ADD LAST_EVENT_DATE DM_DATE;

ALTER TABLE EMPLOYEE ADD LAST_EVENT_TIME DM_TIME;

ALTER TABLE RESIDENT ADD LAST_EVENT_TIME DM_TIME;





