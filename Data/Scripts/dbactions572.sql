ALTER TABLE SHIFT ADD 
MIDNIGHT_BREAKS_OVERTIME_RULE DM_BOOLEAN DEFAULT 0;

UPDATE SHIFT
SET MIDNIGHT_BREAKS_OVERTIME_RULE = 0
WHERE MIDNIGHT_BREAKS_OVERTIME_RULE IS NULL;

ALTER TABLE SHIFT ADD 
PRE_PPH_NIGHT_SHIFT_SPLIT DM_BOOLEAN DEFAULT 0;

UPDATE SHIFT
SET PRE_PPH_NIGHT_SHIFT_SPLIT = 0
WHERE PRE_PPH_NIGHT_SHIFT_SPLIT IS NULL;
